{
    "name": "setup-gcp-for-github-actions",
    "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": {
            "description": "Personal access token for GitHub",
            "required": true
        },
        "GITHUB_REPO_URL": {
            "description": "Repo URL to add GCP secrets to",
            "required": true
        }
    }
}
